import re

with open('src/components/MLImport.tsx', 'r', encoding='utf-8') as f:
    content = f.read()

# Replace the exact block
# It's better to just manually edit `MLImport.tsx` using multi_edit_file or sed.
